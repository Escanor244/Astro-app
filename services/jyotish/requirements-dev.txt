-r requirements.txt

pytest>=8.3
pytest-cov>=5.0

# Required by fastapi.testclient. Undeclared, tests/test_api.py does not merely
# fail -- it errors at collection. Declared here for the same reason as
# jyotishganit below: an undeclared test dependency silently removes coverage.
httpx>=0.27

# Dev-only accuracy oracle. AGPL-3.0 — used ONLY by tests in tests/validation/
# to cross-check our Skyfield engine. It is never imported by shipped code and
# never distributed. See docs/00-orientation.md for why this separation matters.
pyswisseph>=2.10.3.2

# Dev-only cross-check for the divisional charts (MIT). tests/test_vargas.py
# compares our varga rules against this independent implementation.
#
# This MUST stay declared. When it was missing, `pytest.importorskip` at module
# scope silently dropped all 73 varga tests — including the ~50 that never touch
# it — into a single SKIPPED line that `-q` did not print. Reintroducing the
# navamsa boundary bug then gave "194 passed, exit 0": the headline Navamsam
# chart was protected only by an undeclared optional dependency.
jyotishganit>=0.1.3
